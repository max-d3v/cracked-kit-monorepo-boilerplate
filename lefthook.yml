# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  jobs:
    - name: packages audit
      tags:
        - frontend
        - security
      run: bun audit

    - name: build
      run: bun run build

pre-commit:
  jobs:
    - name: ultracite fix
      run: bun run check
      stage_fixed: true
